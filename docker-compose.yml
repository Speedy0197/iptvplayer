services:
  backend:
    build: ./backend
    container_name: iptv-backend
    ports:
      - "8080:8080"
    volumes:
      - iptv_data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/iptv.db
      - PORT=8080
    restart: unless-stopped

volumes:
  iptv_data:
